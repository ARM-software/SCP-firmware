description: ARM PL011 UART

compatible: "arm,pl011"

include: uart-controller.yaml

properties:
    reg:
      required: true

      # MJL: changed to false
    interrupts:
      required: false

    pd-id:
      type: array
      required: false
      description: Power domain index for the component

